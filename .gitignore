node_modules
.next
.vercel
coverage
*.log
*.tsbuildinfo
.env*
# Keep the negation last: gitignore is last-match-wins, so `.env*` above would
# otherwise re-ignore the checked-in template.
!.env.example
.DS_Store
supabase/.temp
